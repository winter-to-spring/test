#!/usr/bin/env bash
# Placeholder — Phase 1 mission will fill in.
# Required: HMAC-SHA256 signature over v0:<ts>:<body> using SLACK_SIGNING_SECRET,
# POST to http://localhost:8000/webhook/slack with X-Slack-Request-Timestamp + X-Slack-Signature headers.
# Usage: ./scripts/seed_slack.sh "message text"
exit 1
