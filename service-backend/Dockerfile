# Placeholder — Phase 1 mission will fill in the build steps.
# Required: python:3.11-slim base, copy app/, install deps from pyproject.toml,
# expose 8000, run uvicorn app.main:app --host 0.0.0.0 --port 8000.
FROM python:3.11-slim
